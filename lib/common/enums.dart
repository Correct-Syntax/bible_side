// Supported translations (versions) of the Bible
enum BibleVersion {
  oetReaders,
  oetLiteral,
  kjv,
}

// Reader screen areas
enum Area {
  top,
  bottom,
}

// Methods of viewing Bibles
enum ViewBy {
  chapter,
  section,
}
