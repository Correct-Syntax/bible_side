// Supported translations of the Bible
enum BibleVersion {
  oetReaders,
  oetLiteral,
  kjv,
}

// Reader screen areas
enum AreaType {
  top,
  bottom,
}

// Methods of viewing Bibles
enum ViewBy {
  chapter,
  section,
}
